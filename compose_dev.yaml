services:

  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    user: "root"
    environment:
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - ./container_data/mssql:/var/opt/mssql:rw
